/**
 * Current terms-of-service version. Bump this when the terms or privacy
 * policy change materially — everyone (logged-in via users.tosAcceptedVersion,
 * anon via localStorage) is then re-prompted by the chat gate, with
 * "updated terms" copy for those who accepted an earlier version.
 *
 * Shared by client and server — keep this file dependency-free.
 */
export const TOS_VERSION = 1;
